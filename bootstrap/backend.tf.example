# Copy this file to backend.tf and fill in the values after bootstrapping.
#
# terraform {
#   backend "s3" {
#     bucket         = "<project>-terraform-state-<region>"
#     key            = "<environment>/<component>/terraform.tfstate"
#     region         = "<region>"
#     dynamodb_table = "<project>-terraform-lock"
#     encrypt        = true
#   }
# }
